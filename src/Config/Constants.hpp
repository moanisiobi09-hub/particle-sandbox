// initialize useful constants that many files will need
#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

constexpr int SCREEN_HEIGHT = 600;
constexpr int SCREEN_WIDTH = 800;

constexpr float GRAVITY = 0.5f;

constexpr int COLLISON_CHECK_FACTOR = 3; // how many times the radius of a particle will be checked for other particles for collision

#endif